---
// Shared section heading: eyebrow, title with one *italic* accent, intro on the right on desktop
import { md, accent } from '../../lib/content';
interface Props { eyebrow?: string; title?: string; intro?: string; center?: boolean }
const { eyebrow, title, intro, center } = Astro.props;
---
{(eyebrow || title || intro) && (
  <div class={`section-head ${title && intro && !center ? 'row' : ''} ${center ? 'center' : ''}`}>
    {eyebrow && <span class="eyebrow">{eyebrow}</span>}
    {title && <h2 data-split set:html={accent(title)} />}
    {intro && <div class="lede" set:html={md(intro)} />}
  </div>
)}
